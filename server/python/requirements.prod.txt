# Production Requirements for Emotional Therapy Service
# This file excludes development tools that require Rust compilation

# Core web framework
flask==2.3.3
flask-cors==4.0.0

# Production server (optional but recommended)
waitress>=2.1.2

# Updated embeddings support
langchain-huggingface>=0.0.1
langchain-text-splitters

# Existing dependencies from emotional_therapy.py
pymongo>=4.6.0
nltk==3.8.1
datasets>=2.20.0
langchain-groq>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0
vaderSentiment==3.3.2
chromadb>=1.2.1  # Vector database for embeddings storage

# ML and NLP
# Note: CPU-only PyTorch is installed separately in build.sh (saves ~2GB vs GPU version)
# torch and torchvision are installed in build.sh before this file

# Embedding models (must install before langchain-huggingface)
sentence-transformers==2.2.2
transformers>=4.40.0,<5.0.0  # Updated to support newer tokenizers
huggingface-hub>=0.33.4,<1.0.0  # Updated to be compatible with langchain-huggingface
tokenizers>=0.19.1  # Required by sentence-transformers

# Utilities
python-dotenv==1.0.0
requests>=2.32.2  # Updated to satisfy datasets requirement

# Core security libraries
cryptography>=41.0.0  # For encryption and key derivation
pycryptodome>=3.19.0  # Additional cryptographic functions

# Input validation and sanitization
bleach>=6.0.0         # HTML sanitization
# html-sanitizer removed - requires Rust compilation and not used (code uses html.escape() and bleach)

# Authentication and session management
PyJWT>=2.8.0          # JWT token handling
passlib>=1.7.4        # Password hashing utilities

# Rate limiting and throttling
slowapi>=0.1.9        # Rate limiting for FastAPI/async
limits>=3.6.0         # General rate limiting

# Secure headers and CORS
secure>=0.3.0         # Security headers management

# Environment and configuration
pydantic>=2.5.0       # Data validation and settings

# Monitoring and logging
structlog>=23.2.0     # Structured logging
python-json-logger>=2.0.7  # JSON log formatting

# Network security
urllib3>=2.0.0        # URL handling with security

# Database security (if using additional DB features)
sqlalchemy>=2.0.0     # ORM with security features

# Optional: Advanced security features
hvac>=1.2.1           # HashiCorp Vault client (for key management)
azure-keyvault-secrets>=4.7.0  # Azure Key Vault (for cloud deployments)

# Performance monitoring (security-related)
psutil>=5.9.0         # System monitoring
memory-profiler>=0.61.0  # Memory usage monitoring

# NOTE: Development tools (bandit, safety, semgrep) are excluded from production
# Install them separately for development: pip install bandit safety semgrep

